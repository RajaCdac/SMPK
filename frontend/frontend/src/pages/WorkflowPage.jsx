export default function WorkflowPage() {
  return (
    <div className="workflow-page">
      <h1>Workflow</h1>
      <p className="text-muted">
        Workflow master, steps, and instance tracking will be configured here.
        The backend workflow models are already in place for future integration.
      </p>
    </div>
  );
}
